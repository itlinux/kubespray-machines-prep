---
- hosts: node1
  become: True
  remote_user: remo
  tasks:
  - name: Put SELinux in permissive mode, logging actions that would be blocked.
    selinux:
      policy: targeted
      state: permissive

